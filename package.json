{
  "name": "pi-rhkb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "esbuild extension/index.ts --bundle --format=esm --platform=node --target=node20 --external:typebox --external:@earendil-works/pi-coding-agent --outfile=dist/rhkb.ts"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.3",
    "@types/node": "^26.6.1",
    "esbuild": "^0.28.1",
    "typebox": "^1.3.28",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
